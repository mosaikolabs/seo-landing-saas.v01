---
// src/pages/thank-you.astro
import Layout from '../layouts/Layout.astro';
import Button from '../components/ui/Button.astro';

// Metadatos SEO
const title = '¡Gracias por contactarnos! | SEO para Joyerías';
const description = 'Hemos recibido tu mensaje. Uno de nuestros especialistas se pondrá en contacto contigo en breve para ayudarte a potenciar tu joyería en línea.';
const canonicalUrl = 'https://tudominio.com/gracias';
---

<Layout 
  title={title}
  description={description}
  canonicalUrl={canonicalUrl}
  noIndex={false}
>
  <div class="min-h-[calc(100vh-16rem)] flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-3xl w-full space-y-8 text-center">
      <div class="mx-auto flex items-center justify-center h-24 w-24 rounded-full bg-green-100">
        <svg class="h-16 w-16 text-green-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
      </div>
      
      <h1 class="text-4xl font-extrabold text-gray-900 dark:text-white sm:text-5xl">
        ¡Mensaje recibido con éxito!
      </h1>
      
      <p class="mt-4 text-xl text-gray-600 dark:text-gray-300">
        Gracias por contactar con nosotros. Hemos recibido tu mensaje y uno de nuestros especialistas en SEO para joyerías se pondrá en contacto contigo en las próximas 24 horas.
      </p>
      
      <div class="mt-10 bg-blue-50 dark:bg-blue-900/30 p-6 rounded-lg border border-blue-100 dark:border-blue-800">
        <h2 class="text-2xl font-bold text-blue-800 dark:text-blue-200 mb-4">¿Qué puedes esperar a continuación?</h2>
        
        <div class="mt-6 space-y-6 text-left max-w-2xl mx-auto">
          <div class="flex items-start">
            <div class="flex-shrink-0">
              <div class="flex items-center justify-center h-8 w-8 rounded-full bg-blue-100 dark:bg-blue-800 text-blue-600 dark:text-blue-200">
                1
              </div>
            </div>
            <div class="ml-4">
              <h3 class="text-lg font-medium text-gray-900 dark:text-white">Revisión de tu caso</h3>
              <p class="mt-1 text-gray-600 dark:text-gray-300">Analizaremos tu situación actual y prepararemos recomendaciones personalizadas para tu joyería.</p>
            </div>
          </div>
          
          <div class="flex items-start">
            <div class="flex-shrink-0">
              <div class="flex items-center justify-center h-8 w-8 rounded-full bg-blue-100 dark:bg-blue-800 text-blue-600 dark:text-blue-200">
                2
              </div>
            </div>
            <div class="ml-4">
              <h3 class="text-lg font-medium text-gray-900 dark:text-white">Llamada de diagnóstico</h3>
              <p class="mt-1 text-gray-600 dark:text-gray-300">Uno de nuestros expertos se pondrá en contacto contigo para entender mejor tus necesidades y objetivos.</p>
            </div>
          </div>
          
          <div class="flex items-start">
            <div class="flex-shrink-0">
              <div class="flex items-center justify-center h-8 w-8 rounded-full bg-blue-100 dark:bg-blue-800 text-blue-600 dark:text-blue-200">
                3
              </div>
            </div>
            <div class="ml-4">
              <h3 class="text-lg font-medium text-gray-900 dark:text-white">Propuesta personalizada</h3>
              <p class="mt-1 text-gray-600 dark:text-gray-300">Recibirás una propuesta detallada con nuestra estrategia para impulsar tu negocio en línea.</p>
            </div>
          </div>
        </div>
      </div>
      
      <div class="mt-10">
        <p class="text-gray-500 dark:text-gray-400 mb-6">
          Mientras tanto, puedes explorar nuestros recursos gratuitos o ponerte en contacto directamente:
        </p>
        
        <div class="flex flex-col sm:flex-row justify-center gap-4">
          <Button 
            href="/#contacto" 
            variant="primary"
            class="w-full sm:w-auto"
          >
            Contactar ahora
          </Button>
          
          <Button 
            href="/blog" 
            variant="outline"
            class="w-full sm:w-auto"
          >
            Ver blog
          </Button>
        </div>
      </div>
      
      <div class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-700">
        <h3 class="text-lg font-medium text-gray-900 dark:text-white">¿Tienes alguna pregunta urgente?</h3>
        <p class="mt-2 text-base text-gray-600 dark:text-gray-300">
          Llámanos al <a href="tel:+34912345678" class="text-primary hover:text-primary/80 font-medium">+34 912 345 678</a> o escríbenos a 
          <a href="mailto:hola@tudominio.com" class="text-primary hover:text-primary/80 font-medium">hola@tudominio.com</a>
        </p>
      </div>
    </div>
  </div>
  
  <script>
    // Redirección automática después de 30 segundos
    document.addEventListener('DOMContentLoaded', () => {
      setTimeout(() => {
        // Guardar en el historial para evitar el bucle de retroceso
        window.history.replaceState(null, '', '/');
        window.location.href = '/';
      }, 30000);
      
      // Evento para Google Analytics
      if (typeof gtag !== 'undefined') {
        gtag('event', 'conversion', {
          'send_to': 'AW-XXXXXXXXX/YYYYYYYYYYYYYY',
          'value': 1.0,
          'currency': 'EUR',
          'transaction_id': ''
        });
      }
      
      // Evento para Facebook Pixel
      if (typeof fbq !== 'undefined') {
        fbq('track', 'Lead');
      }
    });
  </script>
</Layout>

<style>
  /* Animación de aparición */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .animate-fade-in-up {
    animation: fadeInUp 0.6s ease-out forwards;
  }
  
  /* Estilos para la lista de pasos */
  .step-item:not(:last-child) {
    position: relative;
    padding-bottom: 2rem;
  }
  
  .step-item:not(:last-child)::after {
    content: '';
    position: absolute;
    left: 0.75rem;
    top: 2.5rem;
    bottom: 0;
    width: 2px;
    background-color: #e5e7eb;
    z-index: 0;
  }
  
  .dark .step-item:not(:last-child)::after {
    background-color: #374151;
  }
  
  /* Mejoras de accesibilidad */
  .focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }
  
  /* Transiciones suaves */
  .transition-all {
    transition-property: all;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    transition-duration: 150ms;
  }
  
  /* Estilos para modo oscuro */
  @media (prefers-color-scheme: dark) {
    body {
      background-color: #111827;
      color: #f3f4f6;
    }
    
    .bg-blue-50 {
      background-color: rgba(30, 58, 138, 0.2);
    }
    
    .border-blue-100 {
      border-color: rgba(59, 130, 246, 0.3);
    }
  }
</style>
